<%= form_for(@filter_type) do |f| %>
  <% if @filter_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@filter_type.errors.count, "error") %> prohibited this filter_type from being saved:</h2>

      <ul>
      <% @filter_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
