%h1 Pool Types
%table
  %thead
    %tr
      %th Name
  %tbody
    - @pool_types.each do |pool_type|
      %tr
        %td= link_to pool_type.name, edit_pool_type_path(pool_type)        
%ul.actions
  %li= link_to 'New Pool Type', new_pool_type_path, class: 'button'